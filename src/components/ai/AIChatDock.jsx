import React from "react";
import {useEffect,useState} from "react";const suggestions=["Why is Maharashtra growing?","Compare Tier 1 vs Tier 2","Which segment has highest ROI?"];export default function AIChatDock(){const[revealed,setRevealed]=useState(false);const[expanded,setExpanded]=useState(false);useEffect(()=>{const f=e=>{const center=innerWidth/2;const show=e.clientY>=innerHeight-100&&Math.abs(e.clientX-center)<=Math.min(300,innerWidth*.28);if(show)setRevealed(true);else if(!expanded&&e.clientY<innerHeight-180)setRevealed(false)};addEventListener("pointermove",f,{passive:true});return()=>removeEventListener("pointermove",f)},[expanded]);return <div className={`ai-dock ${revealed||expanded?"is-visible":""}`}><div className="ai-dock-border"/><div className="ai-dock-inner"><div className="ai-input-row"><span className="ai-spark">✦</span><input placeholder="Ask a question, or get an insight..."/><button className="ai-send">→</button><button className="ai-close" onClick={()=>{setExpanded(false);setRevealed(false)}}>×</button></div><div className="suggestion-row">{suggestions.map(x=><button key={x} onClick={()=>setExpanded(true)}>{x}</button>)}</div></div></div>}